# Screenshots

These image files are referenced by the main project README:

| File | Screen |
|---|---|
| `landing.png` | The landing / hero page |
| `login.png` | The login page |
| `profile.png` | A user profile page (header + post grid) |

Tips:
- Use PNG, on a clean browser window (no dev tools open).
- Keep the file names exactly as above so the README links resolve.
